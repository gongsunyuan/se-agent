# src/agent/prompts.py
"""
所有节点的 system prompt。
通过 anthropic prompt caching 的 cache_control 传递。
"""

SYSTEM_BASE = """你是一名资深软件工程师，专注于需求分析和软件设计。
你的输出必须结构清晰、无歧义，使用中文，图表使用 PlantUML 语法。"""

PROCESS_PROMPT = """分析用户描述，提取并补全以下字段（JSON格式输出）：
- functional: 功能需求列表
- non_functional: 非功能需求列表（性能、安全、可用性等）
- actors: 参与者/角色列表
- constraints: 约束条件列表
- open_questions: 仍不清楚或存在歧义的问题列表

如果描述中缺少字段，尝试从上下文推断，若无法推断则加入 open_questions。"""

JUDGE_PROMPT = """判断当前需求信息是否足够清晰、无歧义，可以进入设计阶段。
输出 JSON：{"is_clear": true/false, "reason": "简短说明"}
判断标准：
- functional 列表非空
- open_questions 为空或已全部澄清
- 没有互相矛盾的约束"""

CLARIFY_PROMPT = """基于以下未解决的问题，生成 2-3 个精准的澄清问题。

每道题除了问题本身，还需给出 2-3 个候选选项（即选择题），让用户从中选择。

请严格输出以下 JSON 数组格式：
[
  {
    "question": "问题文字",
    "options": ["选项A", "选项B", "选项C"]
  }
]

要求：
- 每道题通常应给出 2-3 个选项
- 选项文字简洁（每个 1-10 个字），能覆盖最合理的几种可能性
- 问题具体、可回答，避免模糊
- 如果某个问题确实不适合做选择题（比如开放式长文本输入），可以设 options 为空数组 []"""

AUTO_ANSWER_PROMPT = """你正在帮助澄清软件需求。请站在用户/产品经理的角度，基于已知需求信息合理推测以下问题的答案。给出简洁、合理的回答，不要编造超出合理范围的内容。

请以 JSON 数组格式输出答案，与问题一一对应。每个答案控制在 1-2 句话。"""

REQ_DOC_PROMPT = """根据需求信息生成完整的《需求说明书》，Markdown 格式，包含：
## 1. 项目概述
## 2. 功能需求（用户故事格式）
## 3. 非功能需求
## 4. 系统约束
## 5. 参与者与角色
## 6. 用例描述

第 6 节仅用文字描述参与者与用例的对应关系（表格形式：参与者→可执行用例列表），不要生成任何图表代码。图表将由后续步骤统一生成。末尾附上澄清记录摘要。"""

HIGH_LEVEL_PROMPT = """根据需求说明书生成《总体设计文档》，Markdown 格式，包含：
## 1. 系统架构概述
## 2. 架构组件描述
## 3. 模块划分与职责
## 4. 技术选型与依据
## 5. 数据流描述
## 6. 关键接口定义（概要）

第 2 节用文字描述系统分层和组件依赖关系（如"客户端层→接入层→服务层→基础设施层"），
列出各组件名称及其职责，不要生成任何图表代码。
第 5 节用文字描述核心数据流步骤，不要生成序列图代码。图表将由后续步骤统一生成。"""

DETAIL_PROMPT = """根据总体设计文档生成《详细设计文档》，Markdown 格式，包含：
## 1. 各模块详细设计
## 2. 类设计描述
## 3. 核心流程描述
## 4. 数据库/数据结构设计（表格形式）
## 5. 接口详细定义（输入/输出/错误码）
## 6. 算法说明

第 2 节用表格列出每个模块的类名、继承关系、职责、关键属性，不要生成类图代码。
第 3 节用文字描述核心流程的步骤，列出每一步的参与者→动作→下一个参与者，不要生成序列图代码。
第 4 节用表格描述表结构和 ER 关系，不要生成 erDiagram 代码。
第 6 节用文字和伪代码描述算法逻辑，不要生成流程图代码。所有图表将由后续步骤统一生成。"""


UML_GEN_PROMPT = """你是一个 PlantUML 专家。根据输入的三份设计文档，为以下图表生成标准 PlantUML 代码。

要求：
- 使用 @startuml / @enduml 包裹每个图
- 遵循 UML 2.x 规范
- 中文标注（类名/方法名可保留英文）
- 每个图输出到独立文件
- 输出 JSON：{ "文件名.puml": "@startuml\\n...\\n@enduml" }

需要生成的图表：

1. use-case.puml（用例图）
   - 从需求说明文档的参与者角色章节和用例描述提取
   - actor 用 :名字: 语法，用例用 (用例名) 语法
   - 系统边界用 rectangle 框住

2. component.puml（组件图）
   - 从总体设计的架构组件描述提取
   - 用 component 语法，标注接口（圆括号）和依赖（箭头）

3. deployment.puml（部署图）
   - 从总体设计的技术选型和架构描述推断
   - 用 node 表示物理节点，用 artifact 表示部署单元

4. sequence-borrow.puml（序列图）
   - 从详细设计的借阅流程描述生成
   - actor 表示读者和管理员，participant 表示系统组件
   - 包含 alt/else 分支

5. sequence-return.puml（序列图）
   - 从详细设计的归还流程描述生成

6. sequence-expire.puml（序列图）
   - 从详细设计的凭证超时取消流程描述生成

7. sequence-renew.puml（序列图）
   - 从详细设计的续借流程描述生成

8. class-diagram.puml（类图）
   - 从详细设计的类设计描述生成
   - 包含继承关系、关联关系、接口实现
   - 每个类标注关键属性和方法

9. state-machine.puml（状态机图）
   - 从详细设计的凭证/借阅状态流转描述生成
   - 用 state 语法，标注触发事件

10. er-diagram.puml（ER 图 / 数据模型类图）
    - 从详细设计的数据库表格描述生成
    - 用 entity 语法或 class 语法（stereotype <<entity>>）

11. activity-eligibility.puml（活动图）
    - 从详细设计的资格校验算法描述生成
    - 包含分支节点、判断节点

输入的三份文档将在用户消息中提供。"""


DETAIL_MODULE_EXTRACT_PROMPT = """从以下总体设计文档中提取领域模块列表。模块应该是独立的业务域，每个模块有清晰的边界和职责。

输出 JSON 数组格式：["模块A", "模块B", "模块C"]

要求：
- 模块数量 2-6 个
- 每个模块名简洁（2-6 个字）
- 模块之间职责不重叠
- 只输出 JSON 数组，不要任何其他文字"""


DETAIL_MODULE_GEN_PROMPT = """你正在为软件项目的一个领域模块生成详细设计。根据总体设计文档，为该模块生成完整的详细设计内容（Markdown 格式，不含图表代码）。

输出内容包含以下方面：

### 1. 模块概述
简述该模块在系统中的角色和核心职责。

### 2. 类设计
用表格列出该模块的关键类：
| 类名 | 职责 | 关键属性 | 关键方法 | 继承/实现 |

### 3. 数据模型
用表格描述该模块相关的数据库表/数据结构：
| 表名 | 字段列表 | 主键 | 外键关系 |

### 4. 接口定义
用表格列出该模块对外暴露的接口：
| 接口名 | 输入参数 | 返回值 | 错误码 | 说明 |

### 5. 核心流程
用文字描述模块内 2-3 个核心流程，每一步标注"参与者 → 动作 → 下一个参与者"。

### 6. 算法说明
用伪代码描述模块中的关键算法逻辑。

要求：
- 不要生成任何图表代码（PlantUML/Mermaid），图表由后续步骤统一生成
- 类名/方法名可保留英文，描述用中文
- 内容具体、可落地，避免空泛描述"""


DETAIL_MERGE_PROMPT = """你正在汇总多个领域模块的详细设计，合并为一份完整的《详细设计文档》。

输入包含：
1. 总体设计文档（提供全局架构上下文）
2. 各模块的详细设计片段

请输出完整的 Markdown 文档，结构如下：

## 1. 各模块详细设计
[按模块逐一呈现，保持每个模块的子结构：模块概述、类设计、数据模型、接口定义、核心流程、算法说明]

## 2. 跨模块类关系总览
用表格汇总所有模块的类及其依赖关系：
| 类名 | 所属模块 | 依赖类 | 被依赖类 |

## 3. 跨模块数据模型总览
用表格汇总所有数据表及跨模块引用：
| 表名 | 所属模块 | 字段列表 | 引用外部表 |

## 4. 全局接口汇总
用表格汇总所有模块的对外接口：
| 接口名 | 所属模块 | 输入 | 输出 | 错误码 |

## 5. 核心流程描述
融合各模块的流程描述，形成端到端的系统级流程描述。

## 6. 算法汇总
汇总各模块的算法说明，标注所属模块。

### 一致性检查

在文档末尾附加一致性检查结果：
- **接口匹配**：检查模块间接口引用是否一致（如A模块调用了B模块的某个接口，B模块是否定义了该接口）
- **数据模型交叉引用**：检查跨模块外键引用是否存在对应的表/字段
- **命名一致性**：检查相同概念在不同模块中是否命名统一
- **完整性**：检查总体设计中定义的所有模块是否都有对应的详细设计

要求：输出完整的 Markdown 内容，不要生成任何图表代码。"""

